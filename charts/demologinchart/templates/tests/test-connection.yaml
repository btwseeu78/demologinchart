apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demologinchart.fullname" . }}-test-connection"
  labels:
    {{- include "demologinchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demologinchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
