apiVersion: v1
kind: Service
metadata:
  name: {{ include "demologinchart.fullname" . }}
  labels:
    {{- include "demologinchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demologinchart.selectorLabels" . | nindent 4 }}
